;(setq i 0)
;(loop
 ;(setq i (+ i 1))
 ;(print i)
 ;(when(>= i 10) (return 0)) 
 ;)

;(loop for x in (list 1 2 3 4)
;      do (print (* x x))
;      )

;(dotimes (x 8)
;    (print (+ x 1))
;    )

;(do (
;     (x 0 (+ 1 x)) 
;     (y 20 (- y 2))
;     )
;    )
; parametre olarak aldığı listedeki 3'e tam bölünen
;sayıların listesini dönderen lambda fonksiyonu
